Core Boldness Implementation Files
1. BOLDNESS_FIX_EXPLANATION.md - Complete Documentation
This file contains the full explanation of how 2D boldness works and was fixed to match 3D rendering.

2. src/App.tsx - Main Boldness Logic
Key sections:

Lines 57-354: Boldness helper functions using Cavalier Contours
Lines 1758-1810: Boldness calculation and application in processTextGroup
Line 1377: globalStrokeWeight: 0 configuration
Lines 1793-1794: Boldness bevel calculation
Line 1815: Boldness included in geometry caching key
Core boldness functions:

typescript
// Lines 304-350: Main boldness application function
function applyBoldnessToShapes(
  shapes: THREE_ACTUAL.Shape[],
  strokeWidth: number
): THREE_ACTUAL.Shape[]
 
// Lines 246-298: Shape offset calculation  
function offsetShape(shape: THREE_ACTUAL.Shape, offset: number): THREE_ACTUAL.Shape
3. src/components/SnowflakePreview.tsx - 2D SVG Boldness
Line 214: 2D boldness application in SVG rendering:

typescript
const strokeWidth = (config.globalStrokeWeight || 0) + (group.thickness || 0);
// Applied as: strokeWidth={strokeWidth} in SVG path
4. src/components/ControlPanel.tsx - Boldness UI Controls
Line 67: UI description for Global Boldness control Line 83: UI description for individual text group Boldness

5. src/translations.ts - Internationalization
Boldness-related translations across all languages (English, Spanish, French, German, Chinese, Japanese):

'Global Boldness' and its description
'Boldness' for individual text groups
6. src/types.ts - Type Definitions
Line 111: globalStrokeWeight: number in SnowflakeConfig interface

7. processTextGroup_FIXED.tsx - Complete Fixed Implementation
This file contains the complete, working implementation of boldness that properly matches 2D and 3D rendering using Cavalier Contours algorithm.

How 2D Boldness Works
2D Rendering (SnowflakePreview.tsx)
typescript
// Line 214: Calculates stroke width for SVG
const strokeWidth = (config.globalStrokeWeight || 0) + (group.thickness || 0);
 
// Line 217: Applied to SVG path
strokeWidth={strokeWidth}
3D Rendering (App.tsx)
typescript
// Lines 1758-1794: Boldness calculation for 3D
const totalStrokeWeight = (rendered3DConfig.globalStrokeWeight || 0) + (textGroup.thickness || 0);
const boldnessBevel = totalStrokeWeight > 0.1 ? totalStrokeWeight / 2 : 0;
 
// Applied via extrusion settings bevel parameters
bevelThickness: bevelPerSide + boldnessBevel,
bevelSize: bevelPerSide + boldnessBevel,
Cavalier Contours Integration (processTextGroup_FIXED.tsx)
typescript
// Lines 46-84: Advanced boldness using path offsetting
const totalStrokeWeight = (rendered3DConfig.globalStrokeWeight || 0) + (textGroup.thickness || 0);
 
if (totalStrokeWeight > 0.1) {
  const offsetDistance = totalStrokeWeight / 2;
  const offsetPolylines = CavalierPathOperations.parallelOffset([polyline], offsetDistance);
  // Convert back to shapes for 3D extrusion
}
The boldness system uses a two-part approach: SVG stroke width for 2D preview and geometric offsetting/beveling for 3D models, ensuring visual consistency between both renderings.