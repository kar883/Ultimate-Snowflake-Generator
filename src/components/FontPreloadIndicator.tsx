import React from 'react';

const FontPreloadIndicator: React.FC = () => {
  // Component is now completely silent - no UI, just background loading
  return null;
};

export default FontPreloadIndicator;
